# Task 6 - Full Regression Verification

## Command Sequence
`yarn lint && yarn test && yarn test:ui && yarn build && npm pack --dry-run`

## Attempt 1

### 1. yarn lint
Status: FAILED
- Error: `src/components/Widget/Widget.tsx:93:17  Make this public static property readonly`
- Action taken: updated `CWidget.contextType` to `public static readonly contextType = ThemeContext;`

## Attempt 2

### 1. yarn lint
Status: PASSED
- ESLint completed successfully

### 2. yarn test
Status: PASSED
- Test Suites: 10 passed, 10 total
- Tests: 111 passed, 111 total

### 3. yarn test:ui
Status: FAILED
- Playwright error: `http://127.0.0.1:5673 is already used, make sure that nothing is running on the port/url or set reuseExistingServer:true in config.webServer.`

### 4. yarn build
Status: NOT RUN
- Blocked because `yarn test:ui` failed

### 5. npm pack --dry-run
Status: NOT RUN
- Blocked because `yarn test:ui` failed

## Scope Evidence
- Added `tests/PublicThemeMatrix.test.tsx` to cover public exported components' theme matrix from package entry.
- Added direct package-entry stability assertion for `Theme`, `CButton`, `CRadio`, `CRadioGroup`, and `CSelect`.
- Updated `CRadio` to apply resolved theme classes on the `.cm-radio` root so theme inheritance/override is directly observable.
- Removed remaining leaf Theme wrapper imports from tests/dev usage in favor of barrel/package entry imports.

## Final Status
PARTIAL PASS
- Code/test changes are in place.
- Lint and Jest pass.
- UI/build/pack remain unverified in a passing run because port `5673` was already occupied during Playwright startup.
