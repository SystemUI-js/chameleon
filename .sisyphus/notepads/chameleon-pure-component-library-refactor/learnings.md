- 2026-03-29: Playwright common-controls/window harness 已统一为纯 `theme + fixture` URL 约定；默认场景也通过 `DevThemeRoot` 挂载，避免 fixture 再走无主题分支。
- 2026-03-29: `tests/ui/common-controls.helpers.ts` 与 `tests/ui/window.helpers.ts` 现仅暴露 theme-only 选择参数，不再保留任何 system/screen 切换语义；`yarn test:ui` 已在纯主题夹具下通过。
- 2026-03-29: 文档更新完成 - README.md 重写为纯组件库描述，移除了 ThemeContext 注册示例；AGENTS.md 更新结构指引指向 src/theme/ 目录；CHANGELOG.md 添加了破坏性 API 重置的 [UnReleased] 条目。
- 2026-03-29: 依赖清理完成 - 移除未使用的 eventemitter3（仅在 package.json/yarn.lock 中存在）；删除遗留的 WindowManager.tsx（引用已删除的 Manager/isManagedConstructor）；@system-ui-js/multi-drag 保留（WindowTitle.tsx 仍在使用）；完整验证通过：yarn lint && yarn test -- --runInBand && yarn test:ui && yarn build。
- 2026-03-29: Jest 覆盖补齐到纯组件接口：新增 `tests/ThemeExports.test.ts` 断言 package entry 暴露纯 theme definitions；`tests/CWindowTitleComposition.test.tsx` 补充 `CWindowBody` 组合断言，避免回归到 shell/system 依赖。
- 2026-03-29: F4 范围审计结论——`src/` 内未检出 `SystemHost/systemType/ScreenManager/WindowManager`、ThemeProvider/runtime registry 或兼容 shim；但检测到 `.sisyphus/plans/chameleon-pure-component-library-refactor.md` 被直接改动（勾选 6/7/8），违反计划文件只读约束，需作为范围偏移处理。
- 2026-03-29: F3 手工 QA 结论——本地 `yarn test:ui` 再次通过（39/39），`yarn build` 通过；组件目录首页实际渲染出 Button/RadioGroup/Select/Window/Dock/StartBar/Grid 七个分区，主题在 `default → win98 → winxp → default` 切换时保持按钮点击、单选、下拉选择状态不丢失，Catalog 内 `CWindow` 标题栏拖拽后位置发生位移、东侧 resize handle 拖拽后宽度从 320 增至 360，满足展示页验收预期。
- 2026-03-29: F4 复核结果——Must Have / Must NOT Have / DoD 全部命令级通过（lint/test/test:ui/build + 关键 grep）；但计划文件 `.sisyphus/plans/chameleon-pure-component-library-refactor.md` 仍处于已修改状态，触发范围保真度 REJECT，需先恢复计划文件只读约束再通过 F4。
- 2026-03-29: F3 最终验证波次补充——`yarn test -- --runInBand` 通过（8/8 suites, 65/65 tests）；`yarn test:ui` 首次执行因 `http://127.0.0.1:5673` 不可达导致 39/39 `ERR_CONNECTION_REFUSED`，手动启动 `yarn dev --host 127.0.0.1` 后复跑 `yarn test:ui` 通过（39/39）；`yarn build` 继续通过，`dist/` 产物完整，Demo 页截图确认正常渲染且无明显报错。
- 2026-03-30: F2 Code Quality Review 结论——`yarn lint`、`yarn build` 均通过；`grep "\\bany\\b" src/**/*.{ts,tsx}` 未发现 `any`；`src/index.ts` 通过 `export * from './components'` 维持完整组件导出面。当前仅剩非阻断提示：ESLint `.eslintignore` 弃用警告、Sass legacy JS API 弃用警告，以及部分 `src/`/`src/dev/` 文件的 Biome import/export 排序 information 级提示。

- 2026-03-30: F1 Plan Compliance 审计结论为 REJECT：任务 1/2/3/5/8 功能面基本达标，但 Task 4 缺少 `catalog-section-*` 测试契约，Task 7 文档仍保留过时 theme model（README ThemeId/Theme 示例与 `src/theme/types.ts` 不一致），且 Task 6 的 `tests/ThemeExports.test.ts` / `tests/CWindowTitleComposition.test.tsx` 尚未完全落入提交历史。
