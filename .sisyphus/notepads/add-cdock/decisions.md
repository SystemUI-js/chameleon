# add-cdock decisions

## 2026-03-22 delegation prep

- Use `openspec-apply-change` for implementation delegations because this is an OpenSpec task-execution session.
- Omit `frontend-ui-ux` for T1 because the first task is API/type/test-contract work, not visual design.
- Omit `playwright` for T1 because this plan uses Jest + RTL rather than browser automation for implementation verification.
