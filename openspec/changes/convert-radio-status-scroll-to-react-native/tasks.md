## 1. 过渡宿主适配

- [x] 1.1 扩展本地 `react-native` 类型和 Jest mock，补齐 radio / scroll / data 属性等当前测试所需的 interop 能力

## 2. 五个组件 RN 化

- [x] 2.1 用 React Native 原生组件重写 `CRadio / CRadioGroup`
- [x] 2.2 用 React Native 原生组件重写 `CStatusBar / CStatusBarItem`
- [x] 2.3 用 React Native 原生组件重写 `CScrollArea`

## 3. 验证

- [x] 3.1 更新并运行受影响测试
- [x] 3.2 运行 `yarn lint`、`yarn test --runInBand` 与 `yarn build`
