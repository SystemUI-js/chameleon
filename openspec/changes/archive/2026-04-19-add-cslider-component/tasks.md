## 1. 组件 API 与值换算

- [x] 1.1 定义 `CSlider` 的 props、局部 `className` 类型与受控值输入约束
- [x] 1.2 实现最小值/最大值裁剪、百分比换算与步进对齐的统一辅助逻辑
- [x] 1.3 在组件中接入值渲染，使滑块按钮位置与填充区域始终跟随当前值更新

## 2. 交互与样式集成

- [x] 2.1 复用 `@system-ui-js/multi-drag` 绑定滑块按钮，并在拖拽过程中复用统一值换算规则
- [x] 2.2 支持点击轨道更新数值，并与拖拽共享相同的边界裁剪与步进对齐逻辑
- [x] 2.3 新增 `CSlider` 样式与稳定 BEM 类名，同时为根节点、轨道、填充区域和滑块按钮附加外部类名

## 3. 导出与验证

- [x] 3.1 在组件目录、`src/components/index.ts` 与 `src/index.ts` 中导出 `CSlider` 及相关类型
- [x] 3.2 新增测试覆盖初始渲染、边界裁剪、步进对齐、拖拽/点击一致性与局部 `className` 注入
- [x] 3.3 运行 `lint`、`test` 与 `build` 验证新增组件可正常集成
