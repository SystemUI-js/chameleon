## 1. 现状确认与定位

- [x] 1.1 复查 Window 组件中 static 模式拖拽/缩放外框的尺寸计算与渲染路径
- [x] 1.2 明确外框尺寸与最终尺寸偏差的触发条件（移动/缩放、边界/最小最大约束）

## 2. 逻辑调整

- [x] 2.1 统一 static 模式外框与最终尺寸的计算来源，复用现有尺寸/约束逻辑
- [x] 2.2 拆分外框渲染尺寸与最终状态提交时机，确保 static 只延迟提交不影响外框数值

## 3. 测试与验证

- [x] 3.1 补充或更新 `tests/Window.test.tsx` 中 static 模式的拖拽/缩放断言
- [x] 3.2 本地运行 `yarn test` 与 `yarn build` 确认无回归
