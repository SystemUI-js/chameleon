## 1. Window 默认视觉样式实现

- [ ] 1.1 审查并整理 `src/components/Window.scss` 现有规则，明确需调整的窗口边框、标题栏与内容区样式范围。
- [ ] 1.2 在 `Window.scss` 实现 Default 主题下的最小可辨识窗口样式：清晰边框、标题栏/内容区层次、活跃态与非活跃态轻量差异。
- [ ] 1.3 确认样式中不包含阴影（`box-shadow`、`filter: drop-shadow` 等）并移除冲突规则。

## 2. 组件结构与行为兼容性检查

- [ ] 2.1 检查 `src/components/Window.tsx` 是否已具备样式所需 class 结构；若不足，仅补充最小语义 className，不改动行为逻辑与公开 API。
- [ ] 2.2 验证 `cm-window`、`cm-window--inactive`、标题栏与内容区结构在渲染输出中的稳定性，确保样式可可靠挂载。

## 3. 测试与文档更新

- [ ] 3.1 在 `tests/Window.test.tsx` 增补/更新断言：窗口结构可识别、活跃态/非活跃态差异可感知、无阴影约束。
- [ ] 3.2 运行并修复相关质量门禁（至少包含 `yarn test`、`yarn lint`、`yarn build`），确保变更可集成。

## 4. 回归验证与交付

- [ ] 4.1 在开发预览中手动核验 Default 主题窗口外观是否满足“简洁、无阴影、能看出是 Window”的目标。
- [ ] 4.2 整理变更说明（影响文件、验证结果、已知风险）用于后续实现与评审。
